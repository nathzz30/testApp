{
    "name": "api-gateway",
    "version": "1.0.0",
    "description": "API Gateway for routing microservices",
    "main": "server.ts",
    "scripts": {
        "start": "ts-node server.ts",
        "dev": "nodemon server.ts",
        "build": "tsc",
        "test": "jest",
        "lint": "eslint . --ext .ts,.js",
        "lint:fix": "eslint . --ext .ts,.js --fix"
    },
    "dependencies": {
        "express": "^4.18.2",
        "http-proxy-middleware": "^2.0.6"
    },
    "devDependencies": {
        "nodemon": "^2.0.22",
        "ts-node": "^10.9.1",
        "typescript": "^5.0.4"
    }
}
